name: bloc_manual_implementation
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  google_fonts:   

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail:
  build_runner:

  flutter_lints: 

flutter:

  uses-material-design: true